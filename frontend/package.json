const express = require('express');
const cors = require('cors');

const app = express();
const PORT = 5000;

app.use(cors());

const products = [
  {
    _id: '1',
    name: 'Fresh Tomatoes',
    description: 'Organic, locally grown tomatoes',
    price: 25,
    image: 'https://via.placeholder.com/150'
  },
  {
    _id: '2',
    name: 'Brown Bread',
    description: 'Healthy whole-grain loaf',
    price: 45,
    image: 'https://via.placeholder.com/150'
  }
];

app.get('/api/products', (req, res) => {
  res.json(products);
});

app.listen(PORT, () => {
  console.log(`✅ API running at http://localhost:5000/api/products`);
});
